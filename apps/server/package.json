{
  "name": "server",
  "version": "0.1.0",
  "description": "a server for out oms",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon --exec ts-node src/index.ts",
    "build": "tsc -b",
    "start": "node dist/index.js",
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@oms/db": "*",
    "@oms/types": "*",
    "@types/express": "^5.0.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@oms/eslint-config": "*",
    "@oms/typescript-config": "*",
    "@types/node": "^22",
    "eslint": "^9.20.0",
    "typescript": "5.7.3"
  }
}
