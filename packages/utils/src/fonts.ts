import { Manrope, Open_Sans } from "next/font/google";
export const manrope = Manrope({ subsets: ["latin"], display: "swap" });
export const openSans = Open_Sans({ subsets: ["latin"], display: "swap", weight: "variable" });
